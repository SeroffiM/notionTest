import { Box, Typography } from '@mui/material';

export const Header = () => {
  return (
    <Box p={2} display="flex" justifyContent="center" component="header">
      <Typography>Header content</Typography>
    </Box>
  );
};
